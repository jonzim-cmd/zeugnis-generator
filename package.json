{
  "name": "zeugnis-generator",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://jonzim-cmd.github.io/zeugnis-generator",
  "dependencies": {
    "@mui/icons-material": "^5.11.0",
    "@mui/material": "^5.11.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "rm -rf docs && mkdir docs && cp -r build/* docs/ && touch docs/.nojekyll",
    "deploy": "npm run build && git add docs && git commit -m \"Update docs folder\" && git push"
  }
}
